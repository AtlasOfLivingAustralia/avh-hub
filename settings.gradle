rootProject.name='avh-hub'
// Inplace plugin config
// Set to true it needed only for development
boolean inplace = false

if (inplace) {
    include 'biocache-hubs'
    project(':biocache-hubs').projectDir = new File('../biocache-hubs')
}