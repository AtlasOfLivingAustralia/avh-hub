<?xml version="1.0" encoding="UTF-8"?>
<web-app version="3.0" metadata-complete="true"
         xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd">
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/applicationContext.xml</param-value>
    </context-param>
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <load-on-startup>2</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <!-- CAS -->
    <context-param>
        <param-name>casServerName</param-name>
        <param-value>https://auth.ala.org.au</param-value>
    </context-param>

    <!-- The Central Server hosting the CMS -->
    <context-param>
        <param-name>centralServer</param-name>
        <param-value>http://www.ala.org.au</param-value>
    </context-param>

    <context-param>
        <param-name>serverName</param-name>
        <param-value>http://localhost:8080</param-value>
    </context-param>

    <!-- for local development -->
    <!-- the context param should be empty if a virtual host is in use -->
    <!--
    <context-param>
        <param-name>contextPath</param-name>
        <param-value>/hubs-webapp</param-value>
    </context-param>
    -->

    <context-param>
        <param-name>uriFilterPattern</param-name>
        <param-value>/admin, /admin/edit*, /admin/upload, /admin/multiUpload, /admin/multiUpload/.*, /admin/optimise, /admin/reloadAllRanks, /admin/loadCaab, /admin/reloadCollections, /admin/reloadInstitutions, /admin/reloadDataProviders, /admin/reloadDataResources</param-value>
    </context-param>

    <context-param>
        <param-name>uriExclusionFilterPattern</param-name>
        <param-value>/images.*,/css.*,/js.*,.*json</param-value>
    </context-param>

    <context-param>
        <param-name>authenticateOnlyIfLoggedInFilterPattern</param-name>
        <param-value>/occurrences/search,/occurrences/.*,/query,/</param-value>
    </context-param>

    <!-- CAS Authentication Service filters -->
    <filter>
       <filter-name>CAS Single Sign Out Filter</filter-name>
       <filter-class>org.jasig.cas.client.session.SingleSignOutFilter</filter-class>
    </filter>

    <filter>
        <filter-name>CAS Authentication Filter</filter-name>
        <filter-class>au.org.ala.cas.client.UriFilter</filter-class>
        <init-param>
            <param-name>filterClass</param-name>
            <param-value>org.jasig.cas.client.authentication.AuthenticationFilter</param-value>
        </init-param>
        <init-param>
            <param-name>casServerLoginUrl</param-name>
            <param-value>https://auth.ala.org.au/cas/login</param-value>
        </init-param>
        <init-param>
            <param-name>gateway</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>

    <filter>
        <filter-name>CAS Validation Filter</filter-name>
        <filter-class>au.org.ala.cas.client.UriFilter</filter-class>
        <init-param>
            <param-name>filterClass</param-name>
            <param-value>org.jasig.cas.client.validation.Cas20ProxyReceivingTicketValidationFilter</param-value>
        </init-param>
        <init-param>
            <param-name>casServerUrlPrefix</param-name>
            <param-value>https://auth.ala.org.au/cas</param-value>
        </init-param>
    </filter>

    <filter>
        <filter-name>CAS HttpServletRequest Wrapper Filter</filter-name>
        <filter-class>au.org.ala.cas.client.UriFilter</filter-class>
        <init-param>
            <param-name>filterClass</param-name>
            <param-value>au.org.ala.cas.client.AlaHttpServletRequestWrapperFilter</param-value>
        </init-param>
    </filter>

    <filter-mapping>
       <filter-name>CAS Single Sign Out Filter</filter-name>
       <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>CAS Authentication Filter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>CAS Validation Filter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>CAS HttpServletRequest Wrapper Filter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Specify the skin to use -->
    <filter>
        <filter-name>skinFilter</filter-name>
        <filter-class>org.ala.hubs.skins.SkinFilter</filter-class>
        <init-param>
            <param-name>barePatterns</param-name>
            <param-value>/fragment/,/annotation/</param-value>
        </init-param>
        <init-param>
            <param-name>skin</param-name>
            <param-value>ala</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>skinFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- SiteMesh -->
    <filter>
        <filter-name>sitemesh</filter-name>
        <filter-class>com.opensymphony.sitemesh.webapp.SiteMeshFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>sitemesh</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Spring ETag Support -->
    <filter>
        <filter-name>etagFilter</filter-name>
        <filter-class>org.springframework.web.filter.ShallowEtagHeaderFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>etagFilter</filter-name>
        <servlet-name>dispatcher</servlet-name>
    </filter-mapping>

    <session-config>
        <session-timeout>1</session-timeout>
    </session-config>
<!--    <welcome-file-list>
        <welcome-file>/index.jsp</welcome-file>
    </welcome-file-list>-->
</web-app>
